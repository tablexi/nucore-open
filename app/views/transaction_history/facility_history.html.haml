= render :partial => "shared/transactions/headers"
- content_for :h1 do
  = @facility
- content_for :h2 do
  %h2
    - if @order_detail_action
      = @order_detail_action.to_s.titleize
    - else
      Transaction History
= content_for :sidebar do
  = render :partial => 'admin/shared/sidenav_billing', :locals => { :sidenav_tab => 'transactions' }
= render :partial => 'shared/transactions/search'
= render :partial => 'shared/transactions/table'  

