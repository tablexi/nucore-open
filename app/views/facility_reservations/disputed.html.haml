= content_for :h1 do
  =h current_facility

= content_for :tabnav do
  = render :partial => 'admin/shared/tabnav_reservation'

- if @order_details.empty?
  %p.notice There are no Disputed reservations
- else
  = render :partial => 'facility_orders/disputed_table'
