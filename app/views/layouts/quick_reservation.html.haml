<!DOCTYPE html>
%html{lang: "en"}
  %head
    = render "/shared/head"
  %body.quick_reservation
    #content{ role: "main" }
      .container
        = render partial: "shared/flashes"

        = yield
    %footer
      %p= request.domain
