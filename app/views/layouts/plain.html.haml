<!DOCTYPE html>
%html
  %head
    = render "/shared/head"
  %body{ class: SettingsHelper.feature_on?(:use_manage) && manage_mode? ? "manage-mode" : "" }
    #content
      .container
        .row
          .span12
            = render "shared/flashes"
            = yield
