= content_for :sidebar do
  = render "admin/shared/sidenav_users", sidenav_tab: "accounts"

= content_for :tabnav do
  = render "admin/shared/tabnav_payment_method", secondary_tab: "members"

= content_for :h1 do
  = current_facility

%h2= @account

= render "accounts/member_table"
