= content_for :h1 do
  = t_my(Reservation)

- if @order_details.empty?
  %p.notice= "You have not made any #{Reservation.model_name.human.pluralize.downcase}."
- else
  = render :partial => 'shared/reservations_table', :locals => { :order_details => @order_details } 

