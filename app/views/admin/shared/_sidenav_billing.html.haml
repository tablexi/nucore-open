%ul
  %li= link_to t('shared.sidenav_billing.accounts'),        facility_accounts_path, :class => (sidenav_tab == 'accounts' ? 'active' : '')
  %li= link_to t('shared.sidenav_billing.receiveable'),    accounts_receivable_facility_statements_path, :class => (sidenav_tab == 'accounts_receivable' ? 'active' : '')
  %li= link_to t('shared.sidenav_billing.notifications'),     facility_notifications_path, :class => (sidenav_tab == 'notifications' ? 'active' : '')
  %li= link_to t('shared.sidenav_billing.review'),       facility_notifications_in_review_path, :class => (sidenav_tab == 'in_review' ? 'active' : '')
  %li= link_to t('shared.sidenav_billing.statements'),      pending_facility_statements_path, :class => (sidenav_tab == 'send_statements' ? 'active' : '')
  %li= link_to t('shared.sidenav_billing.history'),      facility_statements_path, :class => (sidenav_tab == 'statement_history' ? 'active' : '')
  %li= link_to t('shared.sidenav_billing.journals'),        facility_journals_path, :class => (sidenav_tab == 'journals' ? 'active' : '')
  %li= link_to t('shared.sidenav_billing.reconcile.journals'),     history_facility_journals_path, :class => (sidenav_tab == 'reconcile' ? 'active' : '')
  %li= link_to t('shared.sidenav_billing.reconcile.cc'),    credit_cards_facility_accounts_path, :class => (sidenav_tab == 'credit_cards' ? 'active' : '')
  %li= link_to t('shared.sidenav_billing.reconcile.po'), purchase_orders_facility_accounts_path, :class => (sidenav_tab == 'purchase_orders' ? 'active' : '')
