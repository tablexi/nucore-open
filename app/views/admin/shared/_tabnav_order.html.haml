%ul.nav.nav-tabs.tab_counts
  = tab "New / In Process", facility_orders_path, tab_active?(:index)
  - if current_user.manager_of?(current_facility)
    = tab "Problem Orders", show_problems_facility_orders_path, tab_active?(:show_problems)
    = tab "Disputed", disputed_facility_orders_path, tab_active?(:disputed)
    = tab "Add New", new_facility_order_import_path(current_facility), (controller.controller_name == 'facility_orders_import')
