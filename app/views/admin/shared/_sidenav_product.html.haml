%ul.sidebar-nav
  = tab "#{Instrument.model_name.human.pluralize} (#{current_facility.instruments.count})", facility_instruments_path(current_facility), sidenav_tab == Instrument.model_name.plural
  = tab "#{Service.model_name.human.pluralize} (#{current_facility.services.count})", facility_services_path(current_facility), sidenav_tab == Service.model_name.plural
  = tab "#{Item.model_name.human.pluralize} (#{current_facility.items.count})", facility_items_path(current_facility), sidenav_tab == Item.model_name.plural
  = tab "#{Bundle.model_name.human.pluralize} (#{current_facility.bundles.count})", facility_bundles_path(current_facility), sidenav_tab == Bundle.model_name.plural

  - if current_ability.can?(:index, TrainingRequest)
    %li.divider
    = tab "#{TrainingRequest.model_name.human.pluralize} (#{current_facility.training_requests.count})", facility_training_requests_path(current_facility), sidenav_tab == TrainingRequest.model_name.plural
