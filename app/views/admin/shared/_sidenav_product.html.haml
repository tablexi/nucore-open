%ul.sidebar-nav
  = tab "Instruments (#{current_facility.instruments.count})", facility_instruments_path(current_facility), sidenav_tab == 'instruments'
  = tab "Services (#{current_facility.services.count})", facility_services_path(current_facility), sidenav_tab == 'services'
  = tab "Items (#{current_facility.items.count})", facility_items_path(current_facility), sidenav_tab == 'items'
  = tab "Bundles (#{current_facility.bundles.count})", facility_bundles_path(current_facility), sidenav_tab == 'bundles'
