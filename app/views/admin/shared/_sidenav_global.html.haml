%ul.sidebar-nav
  = tab t("pages.affiliates"), affiliates_path, sidenav_tab == "affiliates"
  = tab t("pages.event_log"), log_events_path, sidenav_tab == "log_events"
  = tab t("pages.global_user_roles"), global_user_roles_path, sidenav_tab == "global_user_roles"
  = tab JournalCutoffDate.model_name.human(count: 2), journal_cutoff_dates_path, sidenav_tab == "journal_cutoff_dates"
  = tab JournalCreationReminder.model_name.human(count: 2), journal_creation_reminders_path, sidenav_tab == "journal_creation_reminders"
  = tab text("views.research_safety_certificates.index.title"), research_safety_certificates_path, sidenav_tab == "certificates"
  = tab t("pages.holidays"), holidays_path, sidenav_tab == "holidays"
  = render_view_hook "after", sidenav_tab: sidenav_tab
