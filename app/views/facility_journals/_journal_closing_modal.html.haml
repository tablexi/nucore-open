#journal-closing-reminder.modal.hide.fade{ data: { backdrop: "static" } }
  .modal-header
    = modal_close_button
    %h3= text("facility_journals.journal_closing_modal.header")
  .modal-body
    = reminder_message
  .modal-footer
    %button.btn{ type: "button", data: { dismiss: "modal" } }
      = text("facility_journals.journal_closing_modal.cancel")
    = submit_tag text("facility_journals.journal_closing_modal.proceed"), class: "btn btn-primary", onclick: "event.preventDefault();document.getElementById('journals_create_form').submit();"
