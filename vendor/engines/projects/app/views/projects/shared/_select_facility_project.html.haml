- if facility.projects.active.any?
  %td.action-form
    = select_tag :project_id,
      options_from_collection_for_select(facility.projects.active, :id, :name, ""),
      class: "sync_select",
      include_blank: true
