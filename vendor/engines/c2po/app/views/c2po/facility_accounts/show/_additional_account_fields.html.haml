- if @account.is_a?(CreditCardAccount)
  = f.input :name_on_card
- if @account.is_a?(PurchaseOrderAccount)
  = f.input :outside_contact_info

  = f.input :ar_number

  = render_view_hook "additional_fields", f: f

