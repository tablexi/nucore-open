.well
  %p= t('facilities.manage.payment_options')
  = f.input :accepts_po, :as => :boolean, :label => false, :inline_label => t('facilities.manage.accepts_po')
  = f.input :accepts_cc, :as => :boolean, :label => false, :inline_label => t('facilities.manage.accepts_cc')
