- if account.respond_to?(:splits) && account.splits.present?

  -# TODO: use locales for h3 and table headings
  %h3 Splits

  %table.table.table-striped
    %thead
      %tr
        %th Account
        %th Percent
        %th Extra Penny
    %tbody
      - account.splits.each do |split|
        %tr
          %td= split.subaccount
          %td= split.percent
          %td= t_boolean(split.extra_penny?)
