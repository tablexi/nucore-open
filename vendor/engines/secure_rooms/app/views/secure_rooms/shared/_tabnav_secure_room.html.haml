- if @product.is_a?(SecureRoom) && current_ability.can?(:manage, SecureRooms::CardReader)
  = tab text("title"), facility_secure_room_card_readers_path(current_facility, @product), (secondary_tab == "card_readers")
