- if @product.is_a?(SecureRoom) && current_ability.can?(:manage, SecureRooms::CardReader)
  = tab text("card_readers_title"), facility_secure_room_card_readers_path(current_facility, @product), (secondary_tab == "card_readers")
- if @product.is_a?(SecureRoom) && current_ability.can?(:index, SecureRooms::Occupancy)
  = tab text("occupancies_title"), facility_secure_room_occupancies_path(current_facility, @product), (secondary_tab == "occupancies")
