%ul.nav.nav-tabs
  = tab text("bulk_email.job.create"), bulk_email_cancel_path, secondary_tab == "create"
  = tab text("bulk_email.job.history"), facility_bulk_email_jobs_path, secondary_tab == "history"
